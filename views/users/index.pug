doctype html
html
  head
    title Users List
    link(rel="icon", href="/favicon.ico")
    link(rel="stylesheet", href="/styles.css")
    
  body(class=theme)
    h1 Users
    ul
      each user in users
        li
          a(href=`/users/${user.id}`) #{user.name} (#{user.age})
